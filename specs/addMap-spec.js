'use strict';

var proxyquire = require('proxyquire').noCallThru(),
	stubs = require('./stubs');

describe('addMap', function () {
	it('should add map to DB', function () {

	});

	it('should add map to processing', function () {

	});

	it('should remove map on failure', function () {

	});
});
