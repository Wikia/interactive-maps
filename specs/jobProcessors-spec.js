'use strict';

var proxyquire = require('proxyquire').noCallThru(),
	stubs = require('./stubs');

describe('jobProcessors', function () {
	it('should process a process job', function () {

	});

	it('should process a tiling job', function () {

	});
});
